# Real env file holds DB password and Tailscale auth key — never commit.
.env

# Runtime data. Media (/mnt/tank/immich) and pgData (/mnt/apps/immich) are
# absolute host mounts; the tailscale sidecar state is a RELATIVE bind mount
# created next to this compose file. Never commit any of it.
ts-state/
library/
postgres/
